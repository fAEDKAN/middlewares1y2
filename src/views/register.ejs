<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
	<link rel="stylesheet" href="/css/app.css">

</head>
<body>
    <%- include('./partials/header.ejs') %>
    <h1>Register</h1>
    <form action="" method="POST">

        <label for="firstName">Nombre:</label>
        <input type="text" name="firstName" id="firstName" value="<%= locals.old && old.firstName ? old.firstName : null %>">
        <p class="error"><%= locals.errors && errors.firstName ? errors.firstName.msg : null %></p>

        <label for="lastName">Apellido:</label>
        <input type="text" name="lastName" id="lastName" value="<%= locals.old && old.lastName ? old.lastName : null %>">
        <p class="error"><%= locals.errors && errors.lastName ? errors.lastName.msg : null %></p>


        <label for="email">E-mail:</label>
        <input type="text" name="email" id="email" value="<%= locals.old && old.email ? old.email : null %>">
        <p class="error"><%= locals.errors && errors.email ? errors.email.msg : null %></p>


        <label for="password">Contraseña:</label>
        <input type="text" name="password" id="password" value="<%= locals.old && old.password ? old.password : null %>">
        <p class="error"><%= locals.errors && errors.password ? errors.password.msg : null %></p>


        <button type="submit">Registrarse</button>
    </form>
</body>
</html>